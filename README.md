# Azure DE Project

This project folder is set up for an Azure data engineering workflow.

## Structure

- `data/` for downloaded/raw files
- `notebooks/` for analysis notebooks
- `sql/` for SQL scripts
- `adf/` for Azure Data Factory assets
- `docs/` for project documentation
